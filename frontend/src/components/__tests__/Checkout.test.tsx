import { describe, it } from 'vitest'

describe('Checkout stepper', () => {
  it.skip('renders steps and matches snapshot', () => {
    // Covered by integration tests; skipping due to heavy data requirements.
  })
})
